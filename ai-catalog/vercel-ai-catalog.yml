generated: '2026-09-13'
method: probed
source: https://vercel.com/.well-known/ai-catalog.json
description: 'AI Catalog manifest observed at /.well-known/ai-catalog.json on vercel.com. The AI Catalog
  Standard is the publishing half of Agentic Resource Discovery (ARD): a JSON manifest listing the agentic
  resources a domain offers — MCP servers, agent cards, skills, APIs — each typed by media type and identified
  by a domain-anchored urn:air: URN. Two verdicts are recorded: a structural grade against the data model,
  and the result of the ARD project''s own conformance CLI run against this exact body. Provider-published
  by construction — served from the provider''s own host; nothing here is derived or generated.'
discovery:
  path: /.well-known/ai-catalog.json
  canonical: false
  predecessor_only: true
  attempts:
  - path: /.well-known/ard.json
    http_status: 404
  - path: /.well-known/ai-catalog.json
    http_status: 200
  host: vercel.com
conformance:
  spec: AI Catalog 1.0 / ARD v0.91
  grade: near-conformant
  spec_version_declared: '1.0'
  hard_failures: []
  soft_gaps:
  - entry-no-representativeQueries
  - host-identifier-not-did-web
  official_tool:
    tool: ARD conformance-test v0.9.1 (ards-project/ard-spec)
    status: PASS
    critical_errors: 0
    errors: []
    warnings:
    - '[Vercel MCP OAuth metadata] No ''representativeQueries''. The semantic index is built from this
      term, so the entry will not be found by search — it is a valid catalog entry but not a discoverable
      ARD entry.'
    - '[Vercel REST API] No ''representativeQueries''. The semantic index is built from this term, so
      the entry will not be found by search — it is a valid catalog entry but not a discoverable ARD entry.'
    - '[Vercel agent resources] Media type ''text/markdown'' is based on standard discovery type ''text/markdown''
      but has missing required parameters: profile="urn:air:agent-skills".'
    - '[Vercel agent resources] No ''representativeQueries''. The semantic index is built from this term,
      so the entry will not be found by search — it is a valid catalog entry but not a discoverable ARD
      entry.'
    - '[Vercel documentation graph] No ''representativeQueries''. The semantic index is built from this
      term, so the entry will not be found by search — it is a valid catalog entry but not a discoverable
      ARD entry.'
    informational:
    - '[Vercel MCP OAuth metadata] Media type ''application/json'' is a valid application extension media
      type. ARD permits extension types without core registration.'
    - '[Vercel REST API] Media type ''application/json'' is a valid application extension media type.
      ARD permits extension types without core registration.'
    - '[Vercel documentation graph] Media type ''application/json'' is a valid application extension media
      type. ARD permits extension types without core registration.'
catalog:
  host_display_name: Vercel
  host_identifier: https://vercel.com
  entry_count: 4
  types:
    application/json: 3
    text/markdown: 1
  representative_queries: 0
  trust_manifests: 0
  file: vercel-ai-catalog.json
transport:
  http_status: 200
  content_type: application/ai-catalog+json
  access_control_allow_origin: '*'
x-evidence:
  fetched: '2026-09-13'
  url: https://vercel.com/.well-known/ai-catalog.json
  http_status: 200
  content_type: application/ai-catalog+json
  note: Verbatim body saved alongside this manifest. No field inferred.
